{
  "name": "goobus-cinematic-site",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node tests/route-shell-consistency.mjs && node tests/public-pages-regression.mjs",
    "test:browser": "playwright test",
    "build": "node scripts/build-hostinger.mjs"
  },
  "devDependencies": {
    "@playwright/test": "latest"
  }
}
